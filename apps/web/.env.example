# Optional. In `pnpm dev`, the app uses the Vite proxy to `/trpc` (same origin).
# Set this for `pnpm build` + `pnpm preview` or production when the API is on another origin.
# VITE_API_URL=http://localhost:3001
